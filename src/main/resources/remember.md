/\*\*[^\n]*(\n[^*]*\*[^\*\n]*)*?\n[^\n\*]*\*/


problem:
注解的解析和使用
util的规范化
beanmonitor、processor的规范化和扩展
##############################
tmp:alias 使用
tmp:aop
tmp:util
ConstructorProperties
<entry> Node must specify a key 
bean properties

注解的定义扩展使用

#his
autoware muti type
dependency by anno name
prop resolve
#his

construct-arg 和factorymethod-arg 等效？

beanMonitor  如何监控? aop?

log and exception and spring
